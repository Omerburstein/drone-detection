# ARD100 — processed

Generated by `src.data.prepare_ardmav --dataset ARD100`. Re-derive by deleting
this tree and re-running; `data/raw/` is never modified.

> **Labels only.** Built with `--no-images`: no JPEGs and no
> `data.yaml`. `src.glad_detect` reads the source `.mp4` and
> `src.evaluate --frame-size 1920 1080` needs no pixels, so the GLAD
> path runs off this tree as-is. Re-run without the flag to add the
> images; `data/raw/` is untouched either way.

## Provenance

- **Source:** ARD100, 100 videos / 202,467 frames, Zenodo DOI 10.5281/zenodo.15870538, MD5 b52a5c8aae7b7661b57dde5490c56580 verified.
- **License:** CC-BY-4.0.
- **Raw layout:** `data/raw/ARD100/{videos,Annotations}`, Pascal VOC XML.

## Split rule

**ARD100's own test split, intersected with the videos absent from our local
ARD-MAV 60** — 15 videos, none of them seen by GLAD in training. Same count as
EXP-004's split, so the M4b generalisation comparison is like-for-like in size
and the selection is not a choice we made. ARD100 has 100 videos; 51 overlap our
60, 49 are unseen, and 15 of those 49 are in its test split.

Videos (test): phantom03, phantom92, phantom93, phantom94, phantom95, phantom97, phantom102, phantom110, phantom113, phantom119, phantom133, phantom135, phantom136, phantom141, phantom144

No scene grouping is published for this dataset, so `conditions.json` carries only the measured `lighting` and `relative_range` axes.

## Counts (test)

| | |
| --- | --- |
| Frames | 34287 |
| Boxes | 33517 |
| Frames with no box | 777 |
| Unannotated frames skipped | 0 |

### Target size

| Bucket | Count |
| --- | --- |
| tiny   (<16px) | 20614 |
| small  (16-32) | 12160 |
| medium (32-96) | 730 |
| large  (>96px) | 13 |

## Known issues

- **`CAP_PROP_FRAME_COUNT` overstates**, same as ARD-MAV: the header exceeds
  the XML count by 0–30 frames per video (phantom03 is the one exact match at
  1799). Trust the decoder, not the header.
- **No published scene grouping.** ARD-MAV's `ordinary` / `complex` /
  `small_mav` axis comes from GLAD's paper and has no ARD100 equivalent, so
  `conditions.json` here carries only the measured `lighting` and
  `relative_range` axes. A per-category comparison against EXP-004 is therefore
  not available; the aggregate and the measured axes are.
- **`relative_range` is scaled to this split's own closest approach**, so its
  buckets are *not* comparable to ARD-MAV's. Compare `gt_size` in pixels from a
  `--dump` CSV instead when comparing across datasets.
- **Unlabelled frames are skipped, not emitted as negatives.** A frame with no
  XML was never annotated rather than confirmed empty, so scoring detections
  against it would understate precision. Frames whose XML contains zero objects
  *are* genuine negatives and are kept.
- Frame numbering is one-based and four-digit (`phantom03_0001`), matching the
  XML stems exactly. Verified visually via `_verify/`.
